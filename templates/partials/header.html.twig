
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('homepage') }}">SymBNB !</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('homepage') }}">Accueil</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('ads_index') }}">Annonces</a>
            </li>
        </ul>

        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                   <a href="#" class="nav-link dropdown-toggle active" id="dropdownLink" data-toggle="dropdown">
                        <img class="avatar avatar-mini" src="{{app.user.picture}}" alt="Avatar de {{app.user.fullName}}">
                        {{app.user.fullName}}
                   </a>

                   <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownLink">
                        <a href="{{ path('user_show', {'pseudo': app.user.pseudo}) }}" class="dropdown-item">
                        Mon compte</a>
                        <a href="{{ path('user_bookings') }}" class="dropdown-item">
                        Mes réservations</a>
                        <a href="{{ path('user_comments') }}" class="dropdown-item">
                        Mes commentaires</a>
                        <a href="{{ path('app_edit_user') }}" class="dropdown-item">Modifier mon profil</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('ads_create') }}" class="dropdown-item">Créer une annonce</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('app_logout') }}" class="dropdown-item">Déconnexion</a>
                   </div>
                </li>
            {% else %}
                 <li class="nav-item active">
                    <a href="{{ path("app_register") }}" class="nav-link">Inscription</a>
                </li>
                 <li class="nav-item active">
                    <a href="{{ path("app_login") }}" class="nav-link">Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message|raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}